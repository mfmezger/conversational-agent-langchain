[project]
name = "frontend"
version = "0.1.0"
description = "Frontend for Conversational Agent"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "requests>=2.32.5",
    "streamlit>=1.54.0",
    "httpx>=0.28.1",
]

[tool.uv]
dev-dependencies = []
